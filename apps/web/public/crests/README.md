# Local Crest Assets

This folder stores local badge/crest SVGs used by `teams.crest_url`.

Seed mapping in `apps/api/src/scripts/seed.ts` automatically assigns crest paths, with fallback to:
- `/crests/generic.svg`
